const leftList = [3, 4, 2, 1, 3, 3];
const rightList = [4, 3, 5, 3, 9, 3];

const sortedList = (list) => {
  const result = list.sort((a, b) => a - b);
  console.log(result);
  return result;
};

// função pra calcular a distancia absoluta entre 

// somar as distancias absolutas 
